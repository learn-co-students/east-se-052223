

// 1. find a DOM element => .querySelector()

// 2. find a collection of DOM elements => .querySelectorAll()

// 3. find a DOM element by its ID => .getElementById()

// 4. find a collection of elements by class name => .getElementsByClassName()

// 5. create a new element => .createElement()

// 6. append an element to the DOM => .appendChild()

// 7. remove an element from the DOM => .remove()

// 8. change the text inside an element => .textContent() & .innerText()

// 9. set the HTML inside an element => .innerHTML()